import { addPersons } from './add-persons.transition'

const addKyle = ({ store,  }) => {

  const e = 'e'

  const addPersonTransition = addPersonCreator.create({ 
    args: { persons: [ {name: 'kyle', age: 47, country: 'CA' } ] }
  })
}

export const simulations = {

}