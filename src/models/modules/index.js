import { GTModuleModel } from './module-model.class'
import { GTModule } from './module.class'
import { createModuleModel } from './create-module-model.fn'
import { helpers } from './helpers.utils'

export const modules = {
  GTModuleModel,
  GTModule,
  createModuleModel,
  helpers
}